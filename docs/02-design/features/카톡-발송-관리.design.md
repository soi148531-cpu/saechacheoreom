# Design: 카톡 발송 상태 관리 (청구현황)

**Feature**: 카톡-발송-관리
**Version**: 1.0.0
**Updated**: 2026-04-12
**Architecture**: Option B — 깔끔한 설계 (서비스 모듈 분리)
**Status**: Design ✅

---

## Context Anchor

| Element | Detail |
|---|---|
| **WHY** | 오시오 대체 앱의 핵심 기능 중 하나 (F-04 청구 현황) — 고객에게 제때 청구 메시지를 보내지 못하면 입금 지연 야기 |
| **WHO** | 사장님 (비개발자, 세차소 운영) — 청구 후 누가 발송 안 된 고객인지 빠르게 파악하고 재전송 필요 |
| **RISK** | DB 마이그레이션 누락 시 발송 기록 저장 불가; 카카오 API 인증 정보 노출 위험 |
| **SUCCESS** | 청구 시 카톡 발송 상태 저장/표시 기능 완성 → 카톡 미발송 고객 필터링 가능 |
| **SCOPE** | 청구현황 페이지 수정 (DB, UI, 상태 저장) + 세금신고 데이터 페이지와는 독립 진행 |

---

## 1. 개요

### 1.1 기능 범위

**1단계**: 청구현황 페이지에 카톡 발송 상태 관리
- DB에 `billings.message_sent_at` 컬럼 추가
- 발송 상태 배지 표시 (미발송/발송완료)
- 필터 탭: [전체] [발송완료] [미발송]
- "카톡 발송" 버튼 (상태 기록만, 실제 발송 X)
- **메시지 템플릿 관리**: 설정 페이지에서 메시지 본문 수정 (변수명: `{customer_name}`, `{amount}`, `{car_name}`, `{unit_number}`)

**2단계 이후** (로드맵):
- 이력조회 대시보드 (월별 카톡 발송 리포트)
- 카카오 API 연동 (실제 메시지 발송)

### 1.2 설계 철학

**분리 원칙**:
- 비즈니스 로직 ← `lib/services/messageService.ts`
- UI 컴포넌트 ← `components/MessageBadge.tsx`, `components/MessageButton.tsx`
- 상태 관리 ← `lib/hooks/useMessageFilter.ts`
- 페이지 로직 ← `app/(admin)/billing/page.tsx`

각 계층이 독립적이어서 Phase 2에서 `messageService` 재사용 가능.

---

## 2. 아키텍처 결정사항

### 2.1 선택한 옵션: Option B

**이유**:
- Phase 2 (이력조회 대시보드)에서 `messageService` 재사용 가능
- Phase 3 (카카오 API)에서 서비스 계층만 교체 가능
- 컴포넌트 단위로 테스트 가능

**구조**:
```
MessageService (비즈니스 로직)
  ↓
useMessageFilter (상태 관리)
  ↓
MessageBadge + MessageButton (UI)
  ↓
billing/page.tsx (조합)
```

### 2.2 핵심 결정

| 항목 | 결정 | 근거 |
|---|---|---|
| 메시지 상태 저장소 | DB (Supabase billings 테이블) | 영구 기록, 다른 페이지에서 조회 가능 |
| 발송 기록 | 타임스탬프만 (message_sent_at) | 간단함, 단계별 발송 이력은 로드맵 |
| 필터 위치 | 별도 탭 UI | 결제수단 필터와 독립적, 조합 가능 |
| 버튼 상태 | 항상 활성화 (중복 발송 가능) | 입금 여러 번 받는 경우 재발송 필요 |
| Phase 2 연결 | 같은 `messageService` 사용 | 코드 중복 제거 |

---

## 3. 데이터 모델

### 3.1 DB 스키마

**파일**: `supabase/migrations/15_message_sent_at.sql`

```sql
-- 카톡 발송 상태 추적
ALTER TABLE billings
  ADD COLUMN IF NOT EXISTS message_sent_at TIMESTAMPTZ DEFAULT NULL;

COMMENT ON COLUMN billings.message_sent_at
  IS '카카오 비즈니스 메시지 발송 시각 (NULL = 미발송)';

-- 인덱스 추가 (필터링 성능)
CREATE INDEX IF NOT EXISTS idx_billings_message_sent_at
  ON billings(message_sent_at DESC);

-- 메시지 템플릿 테이블
CREATE TABLE IF NOT EXISTS message_templates (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  template_key TEXT UNIQUE NOT NULL,  -- 'billing_notification' (향후 expand)
  message_body TEXT NOT NULL,  -- "안녕하세요 {customer_name}님, {car_name} 세차 비용 {amount}원 입금 부탁드립니다."
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

COMMENT ON TABLE message_templates IS '카톡 메시지 템플릿 (배포 없이 수정 가능)';
COMMENT ON COLUMN message_templates.template_key IS '템플릿 키 (billing_notification)';
COMMENT ON COLUMN message_templates.message_body IS '메시지 본문 (변수: {customer_name}, {amount}, {car_name}, {unit_number}, {month})';

-- 초기 템플릿 데이터
INSERT INTO message_templates (template_key, message_body)
  VALUES ('billing_notification', '{customer_name}님 - {car_name} {month} 청구액 {amount}원 입금 부탁드립니다.')
  ON CONFLICT (template_key) DO NOTHING;
```

### 3.2 TypeScript 타입

**파일**: `types/index.ts`

```typescript
export interface Billing {
  id: string
  vehicle_id: string
  year_month: string
  wash_count: number
  total_amount: number
  paid_amount: number
  payment_status: 'pending' | 'partial' | 'paid'
  memo: string | null
  sent_at: string | null
  paid_at: string | null
  payment_method: 'cash' | 'card' | 'cash_receipt' | null
  message_sent_at: string | null  // ← NEW
  created_at: string
  vehicle?: Vehicle
  items?: BillingItem[]
}

// 메시지 필터 타입
export type MessageFilter = 'all' | 'sent' | 'unsent'

// 메시지 템플릿
export interface MessageTemplate {
  id: string
  template_key: string  // 'billing_notification'
  message_body: string
  created_at: string
  updated_at: string
}
```

---

## 4. API 설계

### 4.1 Supabase 쿼리 (클라이언트)

**조회 (팩트): 청구 목록 + message_sent_at**
```typescript
const { data } = await supabase
  .from('billings')
  .select(`
    *,
    vehicle:vehicles(*)
  `)
  .eq('year_month', '2026-04')
  .order('created_at', { ascending: false })

// Result:
// [
//   { id, vehicle_id, message_sent_at: "2026-04-12T14:30:00Z", ... },
//   { id, vehicle_id, message_sent_at: null, ... }
// ]
```

**업데이트: 발송 상태 기록**
```typescript
const { error } = await supabase
  .from('billings')
  .update({ message_sent_at: NOW() })
  .eq('id', billingId)

// Response: { data, error }
```

### 4.2 필터 로직 (클라이언트)

**필터 함수**:
```typescript
function filterByMessageStatus(
  billings: Billing[],
  filter: MessageFilter
): Billing[] {
  if (filter === 'sent') {
    return billings.filter(b => b.message_sent_at !== null)
  }
  if (filter === 'unsent') {
    return billings.filter(b => b.message_sent_at === null)
  }
  return billings // 'all'
}
```

---

## 5. 컴포넌트 구조

### 5.1 新컴포넌트

#### `components/MessageBadge.tsx`
```typescript
interface MessageBadgeProps {
  messageSentAt: string | null
}

export function MessageBadge({ messageSentAt }: MessageBadgeProps) {
  if (!messageSentAt) {
    return (
      <span className="inline-flex items-center gap-1 px-2 py-1 text-xs font-semibold bg-red-100 text-red-700 rounded">
        🚫 카톡 미발송
      </span>
    )
  }

  const time = new Date(messageSentAt).toLocaleString('ko-KR', {
    month: '2-digit',
    day: '2-digit',
    hour: '2-digit',
    minute: '2-digit'
  })

  return (
    <span className="inline-flex items-center gap-1 px-2 py-1 text-xs font-semibold bg-gray-100 text-gray-700 rounded">
      ✅ 발송완료 {time}
    </span>
  )
}
```

#### `components/MessageButton.tsx`
```typescript
interface MessageButtonProps {
  billingId: string
  messageSentAt: string | null
  onUpdate?: (billingId: string) => void
}

export function MessageButton({
  billingId,
  messageSentAt,
  onUpdate
}: MessageButtonProps) {
  const [loading, setLoading] = useState(false)

  async function handleSendMessage() {
    setLoading(true)
    const { error } = await updateMessageSentAt(billingId)
    setLoading(false)
    if (!error) {
      onUpdate?.(billingId)
    }
  }

  return (
    <button
      onClick={handleSendMessage}
      disabled={loading}
      className={`px-3 py-1.5 text-sm font-semibold rounded transition-colors ${
        loading
          ? 'bg-gray-100 text-gray-400 cursor-not-allowed'
          : 'bg-blue-600 text-white hover:bg-blue-700'
      }`}
    >
      {loading ? '처리 중...' : messageSentAt ? '재발송' : '카톡 발송'}
    </button>
  )
}
```

#### `components/MessageSettingsPanel.tsx` (NEW)
```typescript
import { useState, useEffect } from 'react'
import { getMessageTemplate, updateMessageTemplate } from '@/lib/services/messageService'
import type { MessageTemplate } from '@/types'

export function MessageSettingsPanel() {
  const [template, setTemplate] = useState<MessageTemplate | null>(null)
  const [editing, setEditing] = useState(false)
  const [body, setBody] = useState('')
  const [loading, setLoading] = useState(false)
  const [saved, setSaved] = useState(false)

  useEffect(() => {
    loadTemplate()
  }, [])

  async function loadTemplate() {
    const data = await getMessageTemplate('billing_notification')
    if (data) {
      setTemplate(data)
      setBody(data.message_body)
    }
  }

  async function handleSave() {
    setLoading(true)
    const { error } = await updateMessageTemplate('billing_notification', body)
    setLoading(false)

    if (!error) {
      setSaved(true)
      setEditing(false)
      loadTemplate()
      setTimeout(() => setSaved(false), 2000)
    }
  }

  return (
    <div className="bg-white border border-gray-200 rounded-xl p-4 max-w-2xl">
      <h3 className="text-lg font-semibold text-gray-900 mb-3">카톡 메시지 템플릿</h3>

      {/* 사용 가능한 변수 */}
      <div className="bg-blue-50 border border-blue-200 rounded-lg p-3 mb-4">
        <p className="text-xs font-semibold text-blue-900 mb-1">사용 가능한 변수:</p>
        <p className="text-xs text-blue-800">
          {'{customer_name}'} (고객명) · {'{car_name}'} (차량명) · {'{amount}'} (금액) ·
          {'{unit_number}'} (호수) · {'{month}'} (월)
        </p>
      </div>

      {/* 템플릿 표시/수정 */}
      {editing ? (
        <div className="space-y-3">
          <textarea
            value={body}
            onChange={e => setBody(e.target.value)}
            rows={4}
            className="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
            placeholder="메시지 템플릿을 입력하세요"
          />
          <div className="flex gap-2 justify-end">
            <button
              onClick={() => {
                setEditing(false)
                setBody(template?.message_body || '')
              }}
              className="px-3 py-1.5 text-sm font-semibold bg-gray-200 text-gray-700 rounded hover:bg-gray-300"
            >
              취소
            </button>
            <button
              onClick={handleSave}
              disabled={loading}
              className="px-3 py-1.5 text-sm font-semibold bg-blue-600 text-white rounded hover:bg-blue-700 disabled:bg-gray-400"
            >
              {loading ? '저장 중...' : '저장'}
            </button>
          </div>
        </div>
      ) : (
        <div>
          <div className="bg-gray-50 border border-gray-200 rounded-lg p-3 mb-3 min-h-20">
            <p className="text-sm text-gray-800">{body}</p>
          </div>
          <button
            onClick={() => setEditing(true)}
            className="px-3 py-1.5 text-sm font-semibold bg-gray-600 text-white rounded hover:bg-gray-700"
          >
            수정
          </button>
        </div>
      )}

      {saved && (
        <div className="mt-3 p-2 bg-green-100 text-green-700 text-sm rounded">
          ✅ 메시지 템플릿이 저장되었습니다
        </div>
      )}
    </div>
  )
}
```

### 5.2 서비스 모듈

#### `lib/services/messageService.ts`
```typescript
import { createClient } from '@/lib/supabase/client'
import type { Billing, MessageTemplate } from '@/types'

const supabase = createClient()

/**
 * 메시지 템플릿 조회
 * @param templateKey - 템플릿 키 ('billing_notification')
 * @returns 템플릿 객체 또는 null
 */
export async function getMessageTemplate(templateKey: string) {
  const { data, error } = await supabase
    .from('message_templates')
    .select('*')
    .eq('template_key', templateKey)
    .single()

  if (error) {
    console.error('Template fetch failed:', error)
    return null
  }
  return data as MessageTemplate
}

/**
 * 메시지 템플릿 업데이트 (관리자용)
 * @param templateKey - 템플릿 키
 * @param messageBody - 새 메시지 본문
 */
export async function updateMessageTemplate(
  templateKey: string,
  messageBody: string
) {
  return await supabase
    .from('message_templates')
    .update({ message_body, updated_at: new Date().toISOString() })
    .eq('template_key', templateKey)
}

/**
 * 메시지 템플릿에 변수 치환
 * @param template - 템플릿 문자열 (예: "{customer_name}님 - {amount}원")
 * @param variables - 치환할 변수 객체 (예: { customer_name: '김철수', amount: '17500' })
 * @returns 치환된 메시지
 */
export function renderMessageTemplate(
  template: string,
  variables: Record<string, string | number | null | undefined>
): string {
  let message = template
  Object.entries(variables).forEach(([key, value]) => {
    const regex = new RegExp(`\\{${key}\\}`, 'g')
    message = message.replace(regex, String(value ?? ''))
  })
  return message
}

/**
 * 청구 정보로 메시지 생성
 * @param billing - 청구 데이터 (vehicle 포함)
 * @param yearMonth - 년월 (예: "2026-04")
 */
export async function buildBillingMessage(
  billing: Billing,
  yearMonth: string
): Promise<string> {
  const template = await getMessageTemplate('billing_notification')
  if (!template) {
    // 폴백 메시지
    return `${billing.vehicle?.customer?.name ?? '고객'}님 - ${billing.total_amount}원 입금 부탁드립니다.`
  }

  const [year, month] = yearMonth.split('-')
  const variables = {
    customer_name: billing.vehicle?.customer?.name ?? '',
    car_name: billing.vehicle?.car_name ?? '',
    amount: billing.total_amount,
    unit_number: billing.vehicle?.customer?.unit_number ?? '',
    month: `${parseInt(month, 10)}월`
  }

  return renderMessageTemplate(template.message_body, variables)
}

/**
 * 발송 상태 업데이트
 */
export async function updateMessageSentAt(billingId: string) {
  return await supabase
    .from('billings')
    .update({ message_sent_at: new Date().toISOString() })
    .eq('id', billingId)
}

/**
 * 메시지 시간 포맷
 */
export function formatMessageTime(messageSentAt: string | null): string {
  if (!messageSentAt) return '미발송'
  const date = new Date(messageSentAt)
  return date.toLocaleString('ko-KR', {
    month: '2-digit',
    day: '2-digit',
    hour: '2-digit',
    minute: '2-digit',
    hour12: false
  })
}

/**
 * 청구 목록 필터
 */
export function filterMessageStatus(
  billings: Billing[],
  filter: 'all' | 'sent' | 'unsent'
): Billing[] {
  if (filter === 'sent') return billings.filter(b => b.message_sent_at !== null)
  if (filter === 'unsent') return billings.filter(b => b.message_sent_at === null)
  return billings
}
```

### 5.3 커스텀 훅

#### `lib/hooks/useMessageFilter.ts`
```typescript
import { useState } from 'react'
import type { MessageFilter, Billing } from '@/types'
import { filterMessageStatus } from '@/lib/services/messageService'

export function useMessageFilter(initialBillings: Billing[]) {
  const [filter, setFilter] = useState<MessageFilter>('all')

  const filtered = filterMessageStatus(initialBillings, filter)

  return {
    filter,
    setFilter,
    filtered,
    counts: {
      total: initialBillings.length,
      sent: initialBillings.filter(b => b.message_sent_at !== null).length,
      unsent: initialBillings.filter(b => b.message_sent_at === null).length
    }
  }
}
```

---

## 6. 상태 관리

### 6.1 billing/page.tsx 상태

```typescript
const [messageFilter, setMessageFilter] = useState<MessageFilter>('all')

// 필터 적용
const filteredBillings = filterMessageStatus(billings, messageFilter)

// 탭 렌더링
const filterTabs: { label: string; value: MessageFilter }[] = [
  { label: `전체 (${messageFilter === 'all' ? filteredBillings.length : '-'})`, value: 'all' },
  { label: `발송완료 (${counts.sent})`, value: 'sent' },
  { label: `미발송 (${counts.unsent})`, value: 'unsent' }
]
```

### 6.2 새로고침 로직

```typescript
async function handleMessageUpdate(billingId: string) {
  // 단일 항목만 새로고침 (효율성)
  const { data } = await supabase
    .from('billings')
    .select('*')
    .eq('id', billingId)
    .single()

  // billings 배열 업데이트
  setBillings(prev =>
    prev.map(b => (b.id === billingId ? data : b))
  )
}
```

---

## 7. 파일 구조

```
새차처럼 코드/
├── supabase/
│   └── migrations/
│       └── 15_message_sent_at.sql                    ← NEW (메시지 템플릿 테이블 추가)
├── types/
│   └── index.ts                                     (수정: MessageTemplate 타입 추가)
├── lib/
│   ├── services/
│   │   └── messageService.ts                        ← NEW (템플릿 조회/수정/변수 치환)
│   └── hooks/
│       └── useMessageFilter.ts                      ← NEW
├── components/
│   ├── MessageBadge.tsx                             ← NEW
│   ├── MessageButton.tsx                            ← NEW
│   └── MessageSettingsPanel.tsx                     ← NEW (메시지 템플릿 수정 UI)
├── app/
│   └── (admin)/
│       ├── billing/
│       │   └── page.tsx                             (수정)
│       └── settings/
│           └── page.tsx                             (수정: MessageSettingsPanel 추가)
└── scripts/
    └── apply-migration.mjs                          (수정)
```

---

## 8. 테스트 계획

### 8.1 단위 테스트

| 테스트 대상 | 시나리오 | 예상 결과 |
|---|---|---|
| `filterMessageStatus()` | filter='unsent', null 포함 배열 | null인 항목만 반환 |
| `formatMessageTime()` | 유효한 ISO 타임스탬프 | 포맷된 시간 문자열 |
| `updateMessageSentAt()` | 유효한 billingId | Supabase 업데이트 성공 |

### 8.2 UI 통합 테스트 (Playwright)

1. **배지 표시**: message_sent_at = null 항목에 "미발송" 배지 표시
2. **필터 탭**: [미발송] 클릭 → null 항목만 표시
3. **버튼 중복 발송**: 발송 완료 항목도 "재발송" 텍스트로 버튼 활성 → 재클릭 시 시각 갱신 확인
4. **버튼 클릭**: "카톡 발송" 클릭 → DB 업데이트 → UI 반영

### 8.3 수동 테스트

1. Migration 실행 후 DB 확인
2. 새 청구 생성 → message_sent_at = NULL 확인
3. 각 필터 탭 클릭 → 목록 필터링 확인
4. "카톡 발송" 버튼 클릭 1차 → 배지 변경 (미발송 → 발송완료) 확인
5. 같은 항목에서 "재발송" 버튼 클릭 2차 → 시각 갱신 확인 (시간이 바뀌어야 함)

---

## 9. 구현 순서

### Phase 1: 기반 (30분)
1. DB 마이그레이션 작성 (`15_message_sent_at.sql` - 메시지 템플릿 테이블 포함)
2. `types/index.ts` 업데이트 (MessageFilter, MessageTemplate types)
3. `scripts/apply-migration.mjs` 업데이트

### Phase 2: 서비스 계층 (45분)
4. `lib/services/messageService.ts` 작성 (템플릿 조회/수정/변수 치환 포함)
5. `lib/hooks/useMessageFilter.ts` 작성

### Phase 3: 컴포넌트 (1시간)
6. `components/MessageBadge.tsx` 작성
7. `components/MessageButton.tsx` 작성
8. `components/MessageSettingsPanel.tsx` 작성 (메시지 템플릿 수정 UI)
9. 스타일 적용 (Tailwind)

### Phase 4: 통합 (1시간)
10. `app/(admin)/billing/page.tsx` 수정
    - messageFilter 상태 추가
    - 필터 탭 UI 렌더링
    - 청구 카드에 MessageBadge, MessageButton 추가
    - handleMessageUpdate() 함수 연결
11. `app/(admin)/settings/page.tsx` 수정 (또는 신규 생성)
    - MessageSettingsPanel 추가

### Phase 5: 테스트 & 배포 (30분)
12. 빌드 검증
13. 수동 테스트 (메시지 템플릿 수정 + 발송)
14. Vercel 배포

**총 예상 시간**: ~4시간

---

## 10. 에러 처리

| 시나리오 | 처리 방식 |
|---|---|
| DB 업데이트 실패 | toast 에러 메시지 + 버튼 상태 복구 |
| 네트워크 오류 | 재시도 버튼 표시 |
| 권한 없음 (401) | 로그인 페이지 리다이렉트 |
| 타임아웃 | "처리 중..." 상태 유지 후 타임아웃 메시지 |

---

## 11. 구현 가이드

### 11.1 DB 마이그레이션 명령

```bash
# 마이그레이션 실행
npm run db:migrate

# 결과 확인
npm run db:check
```

### 11.2 의존성 확인

- ✅ Supabase 클라이언트 (이미 설치)
- ✅ React hooks (이미 설치)
- ✅ Tailwind CSS (이미 설정)
- ✅ TypeScript (이미 설정)

추가 설치 불필요.

### 11.3 Session Guide

이 설계를 구현하기 위한 권장 세션 분할:

| 세션 | 작업 | 예상 시간 |
|---|---|---|
| 세션 1 | Phase 1-2: DB + 서비스 계층 | 1시간 |
| 세션 2 | Phase 3: 컴포넌트 작성 | 1시간 |
| 세션 3 | Phase 4-5: 통합 + 배포 | 1시간 |

---

## Appendix

### A. 참조 코드 (기존)

- `app/(admin)/billing/page.tsx` — 기존 페이지 구조
- `lib/services/billingService.ts` — 참고 (유사 패턴)
- `types/index.ts` — Billing 인터페이스

### B. 다음 단계 (로드맵)

**Phase 2**: 이력조회 대시보드
- `app/(admin)/history/page.tsx` 새 탭 추가
- `messageService` 재사용
- 월별 리포트

**Phase 3**: 카카오 API 연동
- `KAKAO_API_KEY` 환경변수
- `lib/services/messageService.ts` 확장
- 실제 메시지 발송

### C. 배포 체크리스트

- [ ] DB 마이그레이션 실행
- [ ] 타입스크립트 컴파일 성공
- [ ] 빌드 성공 (`npm run build`)
- [ ] 수동 테스트 모두 통과
- [ ] Vercel 배포 (`vercel --prod`)
